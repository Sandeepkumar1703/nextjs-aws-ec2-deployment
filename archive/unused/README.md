This directory contains archived, unused, or optional components moved from the repository root.

Items archived:
- `infra/` — Terraform configuration (not currently used; EC2 was created manually)
- `.github/workflows/ci-cd.yml` — CI/CD workflow (not used; deployments were done manually)
- `scripts/install_docker.sh` — Helper script (EC2 already had Docker installed)

Reason: These files were moved to `archive/unused/` to keep the repository focused on the working application while preserving the original content for future use.

If you want any of these restored to the repo root, move the desired files back and commit.
